{
  "name": "ts-earch-search",
  "version": "0.0.1",
  "main": "build/index.js",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "watch": "tsc -w -p .",
    "build": "tsc -p ."
  },
  "dependencies": {
    "ts-earch-types": "^0.0.1"
  },
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  }
}

